// Прокси к DeepSeek API для дневника питания и ментальной карты, версия для Deno Deploy.
// Ключ и промпты живут только здесь: клиент выбирает режим, но не может подменить текст.

import { runReminders } from './reminders.js';
import { deletePatient } from './admin.js';

// API совместим с OpenAI, поэтому смена провайдера — это смена трёх констант,
// а не переписывание вызова: тело запроса и разбор ответа не меняются.
const DEEPSEEK_URL = Deno.env.get('DEEPSEEK_URL') || 'https://api.deepseek.com/chat/completions';
const DEEPSEEK_API_KEY = Deno.env.get('DEEPSEEK_API_KEY');
// Имя модели вынесено в переменную окружения: если оно окажется неточным,
// правится в панели Deno Deploy без правки кода и без нового деплоя.
const DEEPSEEK_MODEL = Deno.env.get('DEEPSEEK_MODEL') || 'deepseek-v4-flash';
const ACCESS_CODE = Deno.env.get('ACCESS_CODE') || '';
const SYSTEM_PROMPT = Deno.env.get('SYSTEM_PROMPT') || '';

if (!DEEPSEEK_API_KEY) {
  console.error('DEEPSEEK_API_KEY не задан. Локально — в .env, на Deno Deploy — в настройках проекта');
  Deno.exit(1);
}

// Домены, которым разрешено обращаться к API из браузера (через запятую, без слеша на конце).
const ALLOWED_ORIGINS = (Deno.env.get('ALLOWED_ORIGINS') || '')
  .split(',')
  .map((s) => s.trim())
  .filter(Boolean);

// Временно: ментальная карта открывается локально как file://, браузер шлёт Origin: null.
// Пускает к API любую локальную страницу — убрать, когда карта переедет на домен
// и её адрес попадёт в ALLOWED_ORIGINS.
if (!ALLOWED_ORIGINS.includes('null')) ALLOWED_ORIGINS.push('null');

// Режим для дневника питания: собеседник помогает пациенту описать съеденное.
const DIARY_PROMPT = `Ты — помощник в дневнике питания. Твоя задача — помочь человеку описать, что он сегодня ел, чтобы потом посчитать калории и БЖУ.

Как себя вести:
- Задавай короткие уточняющие вопросы о порциях, если человек их не указал. Не проси граммы первым делом: людям проще на глаз. Предложи померить как удобно и подскажи меру, подходящую именно этому блюду — ложки для каши, тарелка для супа, куски для хлеба, горсть для орехов, чашка для напитка. Граммы — лишь один из вариантов на выбор.
- Уточняй способ приготовления, если это влияет на калорийность (жареное/варёное, с маслом или без).
- Отвечай кратко, 1–3 предложения. Человек пишет с телефона.

Если человек говорит, что в этот приём пищи ничего не ел, — не уговаривай и не переспрашивай про порции. Подтверди коротко и сразу поставь [ГОТОВО].

Напитки — полноценный ответ, а не отсутствие ответа. Если человек пил только воду, чай или кофе, не требуй от него еду и никогда не говори, что записывать нечего. Уточни объём (стакан, кружка) и добавки — сахар, молоко, — и этого достаточно.

Когда узнал порции всего названного — не считай сразу. Сначала перечисли, что записал, и спроси, было ли что-то ещё.

Последней строкой ответа напиши ровно [ГОТОВО] — но только в одном из двух случаев:
- человек дал понять, что перечислил всё («всё», «это всё», «больше ничего», «посчитай»);
- он с самого начала описал приём пищи полностью и с порциями, так что спрашивать нечего.

На строке с маркером не должно быть ничего, кроме него. Не ставь [ГОТОВО], пока не знаешь порцию каждого продукта, и не ставь, если только что сам спросил «было ли что-то ещё» и ответа пока нет.

Чего делать нельзя:
- Не давай медицинских советов, не оценивай питание как хорошее или плохое, не рекомендуй диеты и не комментируй вес.
- Если спрашивают о здоровье, лечении или диете — скажи, что это вопрос к лечащему врачу, и вернись к описанию еды.`;

// Режим для ментальной карты: модель предлагает ответвления от выбранного узла.
// Клиент присылает карту целиком и помечает нужный узел, ответ разбирается построчно.
const MINDMAP_PROMPT = `Ты помогаешь декомпозировать цель в ментальной карте. В центре карты — глобальная цель или главная проблема. Каждый уровень вглубь должен быть конкретнее предыдущего, а вся ветка обязана закончиться понятными задачами, которые можно взять и сделать.

Тебе дают карту целиком, путь от центра к нужному узлу и номер его уровня. Узел помечен стрелкой «←». Предлагай ответвления только от него.

ГЛАВНОЕ ПРАВИЛО — ДВИЖЕНИЕ К ЗАДАЧАМ

Предельная глубина карты — 7 уровней. Это потолок, а не цель: чем раньше ветка дойдёт до задач, тем лучше. На каждом шаге спрашивай себя, можно ли уже перейти к задачам, и переходи, как только это возможно.

Насколько ты близок к потолку, настолько конкретнее должны быть формулировки:
- уровни 1–2 — подцели и направления решения, крупные части глобальной цели; задач здесь быть не может, маркер [ЗАДАЧА] на этих уровнях не ставится;
- уровни 3–5 — конкретные результаты и участки работы, у каждого виден исполнитель и осязаемый итог;
- уровни 6–7 — только задачи, новых подцелей здесь быть не может.

На седьмом уровне ты обязан выдать задачи, даже если декомпозиция кажется тебе незавершённой. Лучше грубее нарезанная, но выполнимая задача, чем ещё одна абстрактная подцель.

ЧТО СЧИТАЕТСЯ ЗАДАЧЕЙ

Ответвление — задача, если выполнены все четыре условия:
- её делает один человек;
- она укладывается в срок от часа до нескольких дней;
- результат проверяем: видно «сделано» или «не сделано», без обсуждений;
- она не требует новых решений — только исполнения.

Задачу пиши так: глагол в неопределённой форме + объект + проверяемый результат, затем тире и срок календарной датой в формате «до 15 августа». В конце строки поставь маркер [ЗАДАЧА]. Пример: «Собрать список поставщиков в таблицу — до 20 августа [ЗАДАЧА]».

Сегодняшняя дата приходит в запросе — отсчитывай от неё. Сроки ставь реалистичные и близкие: как правило в пределах двух недель от сегодня. Если задачи в наборе связаны по порядку, разводи даты так, чтобы очередь была видна. Никаких «2 дня», «неделя», «месяц» — только дата, до которой работа сделана.

Задачи не дробятся дальше — маркер означает, что ветка здесь закончена. Не помечай маркером то, что на самом деле требует ещё одного разбора.

КАЧЕСТВО РАЗБИЕНИЯ

- 3–5 ответвлений, не больше;
- они не пересекаются между собой и вместе покрывают родителя целиком;
- каждое отвечает на вопрос «что нужно, чтобы родитель был достигнут», а не «что с ним связано».

Ответ состоит из двух частей, разделённых отдельной строкой из трёх дефисов: ---

Первая часть — рассуждение в 3–5 предложениях, и в нём должен быть виден ход мысли «от глобального к конкретному».

Начни с того, где мы на пути: какой это уровень, насколько узел ещё абстрактен и сколько шагов осталось до задач. Говори об этом как о движении и держись бодрого тона — путь короткий и ведёт к делам с датами, а не к бесконечному дроблению. Например: «Уровень 2, до задач один-два шага: разрезаю по этапам, и уже следующий заход даст дела со сроками» или «Здесь дальше дробить нечего — сразу ставлю задачи с датами».

Дальше по делу: по какому признаку ты разрезал родителя (по этапам, по участникам, по частям результата) и почему именно так, а не иначе; почему этих пунктов достаточно; что отбросил. Если помечаешь строки как задачи — скажи, на основании чего они финальные и почему сроки именно такие.

Без вступлений вроде «Конечно», без пересказа задания и без обещаний «дальше разберём подробнее»: обещать долгий разбор — прямо против сути карты.

Вторая часть — список ответвлений:
- одно ответвление в строке;
- без нумерации, без дефисов, без кавычек, без пояснений до и после списка;
- от 3 до 5 строк;
- подцель — короткая формулировка до шести слов, с заглавной буквы, без точки в конце;
- задача — по формату выше, с маркером [ЗАДАЧА] в конце строки.

Чего избегать:
- не повторяй то, что уже есть в карте, даже другими словами;
- не уходи на уровень выше и не пересказывай сам узел;
- не пиши общие слова вроде «Разное», «Прочие вопросы», «Анализ ситуации»;
- не растягивай декомпозицию ради глубины: лишний уровень абстракции — ошибка, а не осторожность.

Форма ответа целиком (в угловых скобках — что подставить, сами скобки не пиши):
<рассуждение в 2–4 предложениях>
---
<подцель>
<подцель>
<задача — срок [ЗАДАЧА]>

Строка из трёх дефисов должна быть в ответе ровно один раз, и после неё — только ответвления.
В одном ответе подцели и задачи могут соседствовать: помечай маркером те строки, которые ему
отвечают, остальные оставляй без маркера. Не нумеруй строки ни цифрами, ни словами «первое»,
«второе»: это порядковые слова из образца, а не часть ответа.

Отвечай на языке карты и только на нём. Ни иероглифов, ни латиницы, ни слов из других языков —
даже одно такое вкрапление делает ответ непригодным. Если подходящее слово не вспоминается,
подбери простое из языка карты.`;

// Иероглифы и знаки CJK: в русском ответе их быть не может ни при каких условиях.
// Проверка и очистка — разные объекты: у литерала с флагом g test() сдвигает lastIndex.
const CJK_CLASS = "[　-〿぀-ヿ㐀-䶿一-鿿豈-﫿＀-￯]";
const HAS_CJK = new RegExp(CJK_CLASS);
const ALL_CJK = new RegExp(CJK_CLASS, 'g');

// Окно «К чему стремимся» под центром карты: не разбор, а картина результата.
const MINDMAP_GOAL_PROMPT = `Ты помогаешь хозяину ментальной карты увидеть, к чему он идёт.

Тебе дают карту целиком: в центре — глобальная цель, по сторонам — направления, участки работы и задачи.

Опиши идеальное положение дел, которое наступит, когда вся карта будет выполнена. Не план и не список шагов, а картину результата: такую, к которой не стыдно стремиться и которой захочется похвастаться.

Ответ состоит из двух частей, разделённых отдельной строкой ровно из трёх дефисов: ---
Обе части обязательны, и строка из трёх дефисов должна быть в ответе ровно один раз.

ПЕРВАЯ ЧАСТЬ — РАССУЖДЕНИЕ

3–5 предложений, в которых виден ход мысли. Пройдись по карте: какие ветви в ней главные, что получится, когда всё это сделано, по чему будет видно, что цель достигнута. Скажи, что ты берёшь в выжимку, а что оставляешь за скобками и почему — итог короткий, всё в него не влезет. Рассуждение читает человек, который эту карту составил: говори по делу и держись бодрого тона.

Здесь можно писать свободно, но без вступлений вроде «Конечно», без пересказа задания и без обещаний «дальше разберём подробнее».

ВТОРАЯ ЧАСТЬ — САМА ВЫЖИМКА

Одно-два предложения, не больше. Это самая суть: то, ради чего всё затевалось, в одной фразе. Она попадает прямо в окно карты, поэтому:
- настоящее время, как будто всё уже так и есть;
- опирайся на то, что действительно есть в карте, а не на общие слова;
- конкретика вместо пафоса: не «всё работает отлично», а что именно работает и по чему это видно;
- гордость должна быть заслуженной: она держится на выполненном, а не на громких словах;
- никаких списков, заголовков, кавычек и разметки вроде звёздочек и решёток;
- не пересказ рассуждения, а его суть; ни слова о том, что это выжимка.

Лучше одно точное предложение, чем два расплывчатых. Всё, что не поместилось, остаётся в рассуждении — оно не пропадёт, человек его увидит.

Форма ответа целиком (в угловых скобках — что подставить, сами скобки не пиши):
<рассуждение в 3–5 предложениях>
---
<выжимка в одно-два предложения>

Отвечай на языке карты и только на нём. Ни иероглифов, ни латиницы, ни слов из других языков — даже одно такое вкрапление делает ответ непригодным.`;

// Окно «Какая польза и обмен»: зачем то, что человек делает, нужно кому-то ещё.
const MINDMAP_VALUE_PROMPT = `Ты помогаешь хозяину ментальной карты увидеть, кому и чем полезно то, что он делает.

Тебе дают карту целиком: в центре — глобальная цель, по сторонам — направления, участки работы и задачи. Иногда рядом идут соседние окна карты — идеал и базовые принципы; учитывай их, но пересказывать не надо.

Ответь на два вопроса сразу, они об одном: какую пользу получают другие и что приходит взамен ему самому. Обмен — это не только деньги: время, спокойствие, доверие, мастерство, связи, репутация тоже идут в счёт. Если карта о деле для себя — покажи, чем оно всё равно отзывается вовне.

Ответ состоит из двух частей, разделённых отдельной строкой ровно из трёх дефисов: ---
Обе части обязательны, и строка из трёх дефисов должна быть в ответе ровно один раз.

ПЕРВАЯ ЧАСТЬ — РАССУЖДЕНИЕ

3–5 предложений, в которых виден ход мысли. Назови, кто именно получает пользу — не «люди», а те, кто виден в карте: кому это адресовано, кто этим пользуется, кто рядом. Скажи, какую их трудность это снимает и по чему будет видно, что польза настоящая. Отдельно скажи, что возвращается самому автору карты. Скажи, что берёшь в выжимку, а что оставляешь за скобками.

Без вступлений вроде «Конечно», без пересказа задания, без лести.

ВТОРАЯ ЧАСТЬ — САМА ВЫЖИМКА

Одно-два предложения, не больше. В них должны уместиться обе стороны обмена: что получают другие и что получает он. Она попадает прямо в окно карты, поэтому:
- настоящее время, без будущего и без «должно быть»;
- опирайся на то, что действительно есть в карте, а не на общие слова;
- конкретика вместо пафоса: не «приносит огромную пользу», а кому и в чём именно;
- честно: если польза узкая — так и скажи, преувеличенная польза обесценивает всю карту;
- никаких списков, заголовков, кавычек и разметки вроде звёздочек и решёток;
- ни слова о том, что это выжимка.

Форма ответа целиком (в угловых скобках — что подставить, сами скобки не пиши):
<рассуждение в 3–5 предложениях>
---
<выжимка в одно-два предложения>

Отвечай на языке карты и только на нём. Ни иероглифов, ни латиницы, ни слов из других языков — даже одно такое вкрапление делает ответ непригодным.`;

// Окно «Базовые принципы»: скелет, на котором держится работа.
const MINDMAP_RULES_PROMPT = `Ты помогаешь хозяину ментальной карты сформулировать правила, от которых нельзя отходить по дороге к цели.

Тебе дают карту целиком: в центре — глобальная цель, по сторонам — направления, участки работы и задачи. Иногда рядом идут соседние окна карты — идеал и польза; на них опирайся, но не пересказывай.

Принцип — это не совет и не пожелание, а запрет или обязательство, которое держит дело. Проверка простая: если правило можно нарушить и ничего не рухнет — это не принцип. Хороший принцип отвечает на вопрос «чем мы не поступимся, даже когда некогда и тяжело».

Ответ состоит из двух частей, разделённых отдельной строкой ровно из трёх дефисов: ---
Обе части обязательны, и строка из трёх дефисов должна быть в ответе ровно один раз.

ПЕРВАЯ ЧАСТЬ — РАССУЖДЕНИЕ

3–5 предложений. Скажи, где именно у этой карты хрупкое место: что развалится первым, если торопиться или срезать углы. Из этого и выводи правила. Объясни, почему выбрал эти, а не другие, и что отбросил как пожелание.

Без вступлений вроде «Конечно», без пересказа задания.

ВТОРАЯ ЧАСТЬ — САМИ ПРАВИЛА

Правила, каждое с новой строки — их бывает и два, и пять, ровное число не самоцель: лучше два
точных правила, чем три, где одно — вода. Они попадают прямо в окно карты, поэтому:
- одно правило в строке, без нумерации, без дефисов, без кавычек и без пояснений до и после;
- до десяти слов, законченной фразой, с заглавной буквы, без точки в конце;
- формулируй как правило, а не как задачу: «Не берём в работу то, что нельзя проверить», а не «Проверить список»;
- каждое должно быть про эту карту: правило, которое подошло бы любому делу, — пустое;
- правила не повторяют друг друга и не противоречат друг другу;
- никакой разметки вроде звёздочек и решёток.

Форма ответа целиком (в угловых скобках — что подставить, сами скобки не пиши):
<рассуждение в 3–5 предложениях>
---
<правило>
<правило>
<правило>

Отвечай на языке карты и только на нём. Ни иероглифов, ни латиницы, ни слов из других языков — даже одно такое вкрапление делает ответ непригодным.`;

// Окно «Анализ карты»: взгляд со стороны на карту целиком, а не правка одного узла.
// В отличие от остальных режимов карты, ответ не делится на рассуждение и итог —
// клиент кладёт его в окно целиком, поэтому здесь нет строки-разделителя «---».
const MINDMAP_ANALYSIS_PROMPT = `Ты — внешний консультант, разбираешь чужую ментальную карту целиком: центр, обе стороны дерева с задачами и сроками, и соседние окна — базовые принципы, польза и обмен, идеальный результат, признанные трудности.

Логика самой карты, которую нужно держать в голове при разборе: центральное окно — это глобальный замысел, общая цель или глобальная проблема, которую предстоит решить, а всё дерево — её декомпозиция от общего к частному, от большого к малому. У доведённой до конца ветки декомпозиция закономерно приходит к одному из трёх исходов: (1) конкретная задача со сроком, (2) озарение или вывод, который уже можно использовать, (3) осознанно и обоснованно отложенная задача или даже цель — отложенная не потому, что до неё не дошли руки, а по названной причине. Вся конкретика ожидается именно в конечных окнах ветвей, а также в окнах «базовые принципы», «польза и обмен» и «идеальный результат»: если там вместо неё осталась общая формулировка без конкретного наполнения — это и есть слабая сторона, которую нужно назвать.

Отвечай сразу разбором, без вступлений вроде «Конечно» и без пересказа задания. Здесь нет деления на рассуждение и итог — весь твой ответ целиком пойдёт в окно карты, рассуждение не отходы, а сама суть разбора.

Структура ответа — два раздела подряд, каждый начинается с заголовка без разметки и без двоеточия, ровно этими словами:

Сильные стороны
Слабые стороны и рекомендации

В «Сильные стороны» — не короткие ярлыки, а рассуждение: что именно в карте на это указывает (называй конкретные окна и формулировки, а не «в целом»), почему это сильная сторона и что она даёт. Каждый пункт — отдельная строка с тире в начале, но сама строка — законченное рассуждение на одно-два предложения, а не заголовок из нескольких слов.

Во втором разделе слабая сторона и её починка идут парой, а не двумя разными списками: слабостей за раз читают несколько, и если рекомендации к ним свалены в отдельный список в конце, к моменту чтения уже забывается, какая слабость к какой относилась. Порядок такой:
- строка со слабой стороной — тире в начале, рассуждение: что в карте на это указывает, почему это слабость, к чему ведёт, если не менять;
- сразу за ней, если есть чем чинить именно эту слабость, — одна или несколько строк с рекомендацией, каждая начинается со стрелки «→» вместо тире; сказано не только что сделать, но и почему именно так;
- если для слабости прямо сейчас нечего порекомендовать — просто переходи к следующей слабой стороне, лишнюю строку со стрелкой дописывать не нужно.

Рекомендация не бывает сама по себе, без своей слабой стороны прямо над ней: если правка чинит не что-то конкретное из уже названного, а нечто общее — сформулируй это как отдельную слабую сторону и сразу дай к ней рекомендацию, не выноси россыпью в конец раздела.

В каждой строке-пункте (и с тире, и со стрелкой, но не в заголовках разделов) выдели двойными звёздочками **вот так** самую
суть пункта — не всю строку, а 3–8 слов внутри неё, ту часть, ради которой пункт вообще стоит
читать. Одно выделение на строку, не больше. Это не украшение: подсветка идёт человеку, который
пробегает разбор глазами и должен сразу увидеть главное, не читая всё подряд, поэтому выделяй
только по-настоящему ключевое — если выделить нечего, оставь строку как есть.

Требования к разбору:
- опирайся только на то, что реально есть в карте — цитируй формулировки, а не выдумывай контекст;
- если между окнами есть противоречие (например, принципы не согласуются с тем, что реально запланировано) — это слабая сторона, назови её и дай к ней рекомендацию;
- если ветка не доведена ни до задачи со сроком, ни до озарения или вывода, ни до обоснованно отложенного пункта — это слабая сторона: назови, какой из трёх исходов ей нужен и почему сейчас его нет;
- по одному пункту в строке, без нумерации, без вложенных списков, без разметки заголовков и решёток —
  из всей разметки допустимо только двойное выделение **внутри** строки-пункта, как описано выше;
- пиши по существу и разносторонне, так, чтобы после прочтения было ясно, что менять в первую очередь и почему;
- не сокращай ради краткости: подробный разбор с несколькими парами «слабость → рекомендация» в
  каждом разделе ценнее короткого списка из общих фраз — если в карте есть на что посмотреть
  с разных сторон, разбери каждую отдельной парой, а не сворачивай в одну обобщающую строку.

Отвечай на языке карты и только на нём. Ни иероглифов, ни латиницы, ни слов из других языков — даже одно такое вкрапление делает ответ непригодным.`;

const MODE_PROMPTS = {
  diary: DIARY_PROMPT,
  mindmap: MINDMAP_PROMPT,
  'mindmap-goal': MINDMAP_GOAL_PROMPT,
  'mindmap-value': MINDMAP_VALUE_PROMPT,
  'mindmap-rules': MINDMAP_RULES_PROMPT,
  'mindmap-analysis': MINDMAP_ANALYSIS_PROMPT,
};

// Подсчёт итогов по диалогу: модель обязана вернуть строго JSON, а не текст.
// Разбивка по продуктам обязательна — без неё модель склонна возвращать нули.
const SUMMARY_PROMPT = `Ты — анализатор питания. По диалогу ниже посчитай калории и БЖУ.

Сначала распиши КАЖДЫЙ упомянутый продукт отдельно с его весом и пищевой ценностью, затем сложи.

Отвечай СТРОГО одним JSON-объектом, без markdown и без пояснений вне JSON:
{"items": [{"name": "название", "grams": число, "calories": число, "protein": число, "fat": число, "carbs": число}], "summary": "строка", "nothing": true или false, "confident": true или false, "note": "краткий комментарий на русском"}

Правила:
- Считай по стандартным таблицам пищевой ценности продуктов.
- calories в ккал, protein/fat/carbs в граммах, все числа целые и неотрицательные.
- Если вес указан — обязательно посчитай, отказываться нельзя.
- Если вес не указан, возьми типичную порцию и посчитай всё равно.
- confident: false ставь только если продукты вообще не названы.
- Если еда не упоминалась совсем, верни пустой items и confident: false.
- Напитки — такая же запись, как еда, и тоже попадают в items. Вода, чай и кофе без сахара
  и молока идут в items с нулями во всех числах: это не повод вернуть пустой список.
- Пустой items допустим только в двух случаях: человек ничего не ел и не пил (nothing: true)
  либо в диалоге не назван ни один продукт или напиток. Если названо хоть что-то — оно обязано
  попасть в items; при неизвестном объёме бери типичную порцию, а не пропускай.

Поле nothing ставь в true только тогда, когда человек не ел И не пил вообще ничего.
В этом случае верни пустой items и summary ровно "Ничего не ел".
Если он что-то пил — пусть даже одну воду — nothing: false, а напиток попадает в items.

Поле summary — это итоговая запись в дневник питания: только перечисление съеденного и выпитого.
Начинай сразу с продукта. Никаких вводных слов о себе — ни «Я ел», ни «Я съел», ни «Съедено», ни «На обед».
В неё должны войти ВСЕ подробности, которые человек назвал за весь разговор, включая уточнённые не сразу:
количество и меру порции его словами (пять столовых ложек, средняя тарелка, два куска, одна штука),
способ приготовления, добавки и прямо названное их отсутствие.
Ничего не выбрасывай и не обобщай: «порция каши» вместо «пять столовых ложек каши» — ошибка.
Меру относи ровно к тому продукту, к которому отнёс её человек. Если он назвал количество отдельной
репликой в ответ на вопрос о главном блюде, оно относится к главному блюду, а не к добавке.
Когда непонятно, к чему относится количество, оставь его отдельной частью фразы, как сказал человек,
и не приписывай ни одному продукту.
Не добавляй своего: ни пересчитанных граммов, ни калорий, ни белков, жиров и углеводов. Числа в summary
допустимы только те, что назвал сам человек.
Пример: "Рисовая каша на молоке без сахара со сливочным маслом, пять полных столовых ложек."`;

// Модель иногда всё равно начинает с «Я ел». Срезаем зачин и поднимаем первую букву.
function stripLeadIn(text) {
  // \b с кириллицей в JS не работает: конец слова задаём вручную.
  const cleaned = text.replace(
    /^\s*(?:я\s+)?(?:ел|ела|съел|съела|ем|поел|поела|пил|пила|выпил|выпила|употребил|употребила|скушал|скушала)(?=[\s:,-]|$)[\s:,-]*/i,
    ''
  );
  if (cleaned === '' || cleaned === text) return text;
  return cleaned.charAt(0).toUpperCase() + cleaned.slice(1);
}

function corsHeaders(request) {
  const origin = request.headers.get('origin');
  if (!origin || !ALLOWED_ORIGINS.includes(origin)) return {};
  return {
    'Access-Control-Allow-Origin': origin,
    Vary: 'Origin',
    'Access-Control-Allow-Headers': 'Content-Type, x-access-code',
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
  };
}

function json(data, status, headers) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'Content-Type': 'application/json; charset=utf-8', ...headers },
  });
}

// Простой rate limit по IP, чтобы случайный визитёр не сжёг лимиты API.
// Счётчик живёт в памяти изолята: Deploy держит их по регионам, так что лимит
// получается мягким — как и было на Render. Это заслон от случайностей, не защита.
const RATE_LIMIT = 20; // запросов
const RATE_WINDOW_MS = 60 * 1000; // за минуту
const hits = new Map();

function isRateLimited(ip) {
  const now = Date.now();
  const entry = hits.get(ip) || { count: 0, windowStart: now };
  if (now - entry.windowStart > RATE_WINDOW_MS) {
    entry.count = 0;
    entry.windowStart = now;
  }
  entry.count += 1;
  hits.set(ip, entry);
  return entry.count > RATE_LIMIT;
}

// Код доступа нужен для чужих клиентов. Запросы со своих доменов (ALLOWED_ORIGINS)
// пропускаем без него — иначе код пришлось бы зашить в открытый код страницы,
// где он всё равно перестал бы быть секретом.
function isAuthorized(request) {
  if (!ACCESS_CODE) return true;
  if (request.headers.get('x-access-code') === ACCESS_CODE) return true;
  const origin = request.headers.get('origin');
  // Origin: null подделывается одной строкой в curl, поэтому послаблением он не пользуется:
  // локальная ментальная карта присылает код заголовком, как чужой клиент.
  return Boolean(origin) && origin !== 'null' && ALLOWED_ORIGINS.includes(origin);
}

async function readMessages(request) {
  let body;
  try {
    body = await request.json();
  } catch {
    return { error: 'Тело запроса должно быть корректным JSON' };
  }
  if (!Array.isArray(body?.messages) || body.messages.length === 0) {
    return { error: 'Поле messages обязательно и должно быть непустым массивом' };
  }
  return { messages: body.messages, mode: body.mode, stream: body.stream === true };
}

async function callDeepSeek(payload) {
  const response = await fetch(DEEPSEEK_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${DEEPSEEK_API_KEY}`,
    },
    // thinking: disabled — без этого модель по умолчанию долго «размышляет» про
    // себя (см. reasoning_content) прежде чем выдать хоть слово итогового ответа;
    // ни один наш промпт видимых рассуждений не просит, нужен только прямой ответ.
    body: JSON.stringify({ model: DEEPSEEK_MODEL, thinking: { type: 'disabled' }, ...payload }),
  });
  const data = await response.json();
  return { response, data };
}

// Потоковый вариант — см. подробное объяснение в grok-chat-cf/main.js (тот же
// приём, портировался оттуда). Вкратце: для тяжёлых запросов (разбор карты
// целиком) ответ пробрасывается клиенту по мере генерации, а не одним блоком
// в конце — это защита от обрыва по простою на длинных генерациях, а не только
// про скорость. Автоповтор при иероглифах (см. handleChat) на потоковом пути
// не работает — единственная защита от них здесь остаётся в самом промпте.
async function streamDeepSeek(payload, cors) {
  const response = await fetch(DEEPSEEK_URL, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${DEEPSEEK_API_KEY}`,
    },
    body: JSON.stringify({ model: DEEPSEEK_MODEL, thinking: { type: 'disabled' }, stream: true, ...payload }),
  });

  if (!response.ok || !response.body) {
    let message = 'Ошибка DeepSeek API';
    try {
      const data = await response.json();
      message = data.error?.message || message;
    } catch {
      // тело могло быть пустым или не-JSON — оставляем сообщение по умолчанию
    }
    return json({ error: message }, response.status || 502, cors);
  }

  return new Response(response.body, {
    status: 200,
    headers: {
      ...cors,
      'Content-Type': 'text/event-stream; charset=utf-8',
      'Cache-Control': 'no-store',
    },
  });
}

function toNonNegativeInt(value) {
  const num = Math.round(Number(value));
  return Number.isFinite(num) && num > 0 ? num : 0;
}

// Сумму считаем сами: арифметике модели доверять нельзя.
function sumItems(items) {
  const totals = { calories: 0, protein: 0, fat: 0, carbs: 0 };
  if (!Array.isArray(items)) return totals;
  for (const item of items) {
    if (!item || typeof item !== 'object') continue;
    totals.calories += toNonNegativeInt(item.calories);
    totals.protein += toNonNegativeInt(item.protein);
    totals.fat += toNonNegativeInt(item.fat);
    totals.carbs += toNonNegativeInt(item.carbs);
  }
  return totals;
}

async function handleChat(request, cors) {
  const { messages, mode, stream, error } = await readMessages(request);
  if (error) return json({ error }, 400, cors);

  // mode выбирает серверный промпт; без него работает SYSTEM_PROMPT из настроек
  const systemPrompt = MODE_PROMPTS[mode] || SYSTEM_PROMPT;
  const fullMessages = systemPrompt
    ? [{ role: 'system', content: systemPrompt }, ...messages]
    : messages;

  if (stream) return streamDeepSeek({ messages: fullMessages }, cors);

  const { response, data } = await callDeepSeek({ messages: fullMessages });
  if (!response.ok) {
    console.error('DeepSeek API error:', JSON.stringify(data));
    return json({ error: data.error?.message || 'Ошибка DeepSeek API' }, response.status, cors);
  }

  let reply = data.choices?.[0]?.message?.content ?? '';

  // Модель изредка роняет в русский текст иероглифы. Промптом это не лечится,
  // поэтому одна повторная попытка, а если и она с мусором — вычищаем символы.
  if (typeof mode === 'string' && mode.startsWith('mindmap') && HAS_CJK.test(reply)) {
    const retry = await callDeepSeek({ messages: fullMessages });
    const second = retry.response.ok ? retry.data.choices?.[0]?.message?.content ?? '' : '';
    reply = second && !HAS_CJK.test(second) ? second : reply.replace(ALL_CJK, '');
  }

  return json({ reply }, 200, cors);
}

async function handleSummary(request, cors) {
  const { messages, error } = await readMessages(request);
  if (error) return json({ error }, 400, cors);

  const { response, data } = await callDeepSeek({
    messages: [{ role: 'system', content: SUMMARY_PROMPT }, ...messages],
    response_format: { type: 'json_object' },
    temperature: 0,
  });

  if (!response.ok) {
    console.error('DeepSeek API error:', JSON.stringify(data));
    return json({ error: data.error?.message || 'Ошибка DeepSeek API' }, response.status, cors);
  }

  const raw = data.choices?.[0]?.message?.content ?? '';
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    console.error('Модель вернула не JSON:', raw);
    return json({ error: 'Не удалось разобрать ответ модели' }, 502, cors);
  }

  const totals = sumItems(parsed.items);
  const items = Array.isArray(parsed.items)
    ? parsed.items
        .filter((item) => item && typeof item === 'object')
        .map((item) => ({
          name: typeof item.name === 'string' ? item.name : 'продукт',
          grams: toNonNegativeInt(item.grams),
          calories: toNonNegativeInt(item.calories),
          protein: toNonNegativeInt(item.protein),
          fat: toNonNegativeInt(item.fat),
          carbs: toNonNegativeInt(item.carbs),
        }))
    : [];

  return json(
    {
      ...totals,
      items,
      // Итоговая запись для дневника — её показывают врачу вместо цифр.
      summary: typeof parsed.summary === 'string' ? stripLeadIn(parsed.summary.trim()) : '',
      // Человек прямо сказал, что не ел: нулевой итог здесь законный, не ошибка разбора.
      nothing: parsed.nothing === true && items.length === 0,
      confident: parsed.confident === true && items.length > 0,
      note: typeof parsed.note === 'string' ? parsed.note : '',
    },
    200,
    cors
  );
}

async function handler(request, info) {
  const url = new URL(request.url);
  const cors = corsHeaders(request);

  if (request.method === 'OPTIONS') {
    return new Response(null, { status: 204, headers: cors });
  }

  // Дешёвая проверка живости, без авторизации и лимитов: ответ не зависит от DeepSeek.
  if (url.pathname === '/health') {
    return json({ ok: true }, 200, {
      'Cache-Control': 'no-store',
      'Access-Control-Allow-Origin': '*',
    });
  }

  // Ручной прогон напоминаний — для проверки, обычно их запускает Deno.cron.
  if (url.pathname === '/api/reminders/run') {
    const secret = Deno.env.get('REMINDERS_SECRET') || '';
    if (!secret || request.headers.get('x-reminders-secret') !== secret) {
      return json({ error: 'Нет доступа' }, 401, cors);
    }
    try {
      return json(await runReminders(), 200, cors);
    } catch (err) {
      console.error('Напоминания:', err);
      return json({ error: String(err) }, 500, cors);
    }
  }

  // Удалить пациента целиком: профиль, все записи, вход. Необратимо, только по одному uid за раз.
  if (url.pathname === '/api/admin/delete-patient' && request.method === 'POST') {
    const secret = Deno.env.get('REMINDERS_SECRET') || '';
    if (!secret || request.headers.get('x-reminders-secret') !== secret) {
      return json({ error: 'Нет доступа' }, 401, cors);
    }
    try {
      const body = await request.json();
      const result = await deletePatient(body.uid);
      return json(result, 200, cors);
    } catch (err) {
      console.error('Удаление пациента:', err);
      return json({ error: String(err) }, 500, cors);
    }
  }

  const isApi = url.pathname === '/api/chat' || url.pathname === '/api/nutrition-summary';
  if (!isApi) return json({ error: 'Не найдено' }, 404, cors);
  if (request.method !== 'POST') return json({ error: 'Ожидается POST' }, 405, cors);
  if (!isAuthorized(request)) return json({ error: 'Неверный код доступа' }, 401, cors);

  const ip = request.headers.get('x-forwarded-for')?.split(',')[0].trim() ||
    info?.remoteAddr?.hostname || 'unknown';
  if (isRateLimited(ip)) {
    return json({ error: 'Слишком много запросов, попробуйте позже' }, 429, cors);
  }

  try {
    return url.pathname === '/api/chat'
      ? await handleChat(request, cors)
      : await handleSummary(request, cors);
  } catch (err) {
    console.error('Ошибка запроса к DeepSeek:', err);
    return json({ error: 'Не удалось связаться с DeepSeek API' }, 502, cors);
  }
}

// Раз в минуту: у каждого пациента свой часовой пояс, поэтому «13:00 у него»
// нельзя поймать почасовым расписанием — проверяем часто и шлём один раз за день.
if (typeof Deno.cron === 'function' && Deno.env.get('FIREBASE_SERVICE_ACCOUNT')) {
  Deno.cron('nutrition-reminders', '* * * * *', async () => {
    try {
      await runReminders();
    } catch (err) {
      console.error('Напоминания:', err);
    }
  });
}

Deno.serve({ port: Number(Deno.env.get('PORT')) || 8000 }, handler);
